root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/app ./cmd/app"
  bin = "./tmp/app"
  args_bin = ["serve"]
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "db/migrations"]
  delay = 300

[log]
  time = true

[color]
  main = "magenta"
  watcher = "cyan"
  build = "yellow"
  runner = "green"
